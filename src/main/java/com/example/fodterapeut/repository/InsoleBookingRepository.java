package com.example.fodterapeut.repository;

import com.example.fodterapeut.model.InsoleBooking;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InsoleBookingRepository extends JpaRepository<InsoleBooking, Integer> {
}
